# Deprecated: DOM Qualifiers

This document is obsolete.

It describes an older DOM / bridge / introspector model that is no longer the
authoritative direction for Axis and Protomorph.

The active migration plan now lives in:

- `packages/protomorph/docs/manual/pm-axis-roadmap.md`

That roadmap is the current source of truth for:

- qualifier algebra and qualifier-specific behavior
- host / database responsibilities
- matching, substitution, and dispatching
- convergent `Result[E] T` modeling

The detailed qualifier specification will be rewritten in the Protomorph manual,
not in the Axis `docs/` tree.
