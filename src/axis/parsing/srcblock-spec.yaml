doc:
  description: 'Documentation'
  keyword: '#'

doc > doc:

unit:
  description: 'Source unit'
unit | doc:
unit | fn:
unit | val:
unit | def:

def:
def | doc:
def | takes:
def | where:
def | as:

as:
as | doc:
as > val:

fn:
fn | doc:
fn | takes:
fn | returns:
fn | where:
fn | suite:

takes:
takes | doc:
takes > val:

val:
val | doc:

where:
where > val:

returns:
returns | doc:
returns > val:

suite:

using:



