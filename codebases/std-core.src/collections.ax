
def Map
# the Map entity

def Map[kemap]
takes: 
    val keymap: V -> K
where:
    val V: Type
    val K: Type
with:
    val Keymap = index_of(K)
    # the index of all keys in K

def Set
# un set se comporta como un : Map[x->x]



def a + b
# la operacion de suma aritmetica
takes:
    val a: N
    val b: N
where:
    val N: Number
