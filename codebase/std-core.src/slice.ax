unit std

def Slice T
    -------

where:
    val T: Number
takes:
    val start: T
        --------
        Start value

    val stop: T
        -------
        Stop value

    val step: Positive T = 1
        --------------------
        Step 


        rules:
            




with:
    val sign = if (start >= step) {+1} else {-1}
    val delta = sign * step

